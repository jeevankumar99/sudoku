1. strcmp is used to compare two strings, when it returns 0, it means that the two strings it compared are similar or the same.

2. int max;
   if(g.level == "debug")
    max = 9;
   else
    max = 1024;

3. If the number entered by the user is not an integral.

4. y and x.

5. void reposition

6. 187 or any other line inside the switch statement.

7. 1 in debug #1, 2 in debug #2, 9 in debug #9.


